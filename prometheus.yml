global:
  scrape_interval: 15s # How often to ping your API

scrape_configs:
  - job_name: 'uptime-monitor-api'
    static_configs:
      # "api" is the name of your service in docker-compose!
      - targets: ['api:8000']